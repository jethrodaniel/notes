<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || t("shared.application_title") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="<%= image_url("icon.png") %>" type="image/png">
    <link rel="icon" href="<%= image_url("icon.svg") %>" type="image/svg+xml">
    <link rel="apple-touch-icon" href="<%= image_url("icon.png") %>">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="w-screen h-screen flex flex-col items-center">
    <%= yield %>
  </body>
</html>
