<%# locals: (hidden: true) -%>
<%= tag.div \
  id: dom_id(Note, :empty_note_discarded),
  data: hidden ? {} : {
    controller: "auto-remove",
    auto_remove_delay_value: 5000
  },
  class: "#{hidden and 'hidden'} p-4 bg-zinc-50 dark:bg-zinc-900 shadow border-y md:border md:rounded-lg border-zinc-400 dark:border-zinc-600 shadow hover:bg-zinc-200 dark:hover:bg-zinc-800" do %>
  <%= t("notes.empty_note_discarded") %>
<% end %>
